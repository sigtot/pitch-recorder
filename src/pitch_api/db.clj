(ns pitch-api.db)

(def pg-db {:dbtype   "postgresql"
            :dbname   "notes"
            :host     "db"
            :user     "postgres"
            :port     5432
            :password "pass"})

