(ns hello-api.db)

(def pg-db {:dbtype   "postgresql"
            :dbname   "notes"
            :host     "localhost"
            :user     "postgres"
            :port     5469
            :password "pass"})

